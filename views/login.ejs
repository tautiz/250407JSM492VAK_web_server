<%- include('partials/head', { title: 'Prisijungimas', activePage }) %>
<%- include('partials/header', { activePage }) %>
    <div class="auth-container">
        <div class="auth-header">
            <h1>Prisijungimas</h1>
            <p>Prisijunkite prie savo paskyros</p>
        </div>
        <% if (typeof error !== 'undefined') { %>
            <div class="error-message">
                <%= error %>
            </div>
        <% } %>
        
        <form action="/login" method="post" class="auth-form">
            <div class="form-group">
                <label for="email">
                    <i class="icon">📧</i>
                    El-pašto adresas
                </label>
                <input type="email" id="email" name="email" placeholder="vardas@pavyzdys.lt" required>
            </div>
            
            <div class="form-group">
                <label for="password">
                    <i class="icon">🔒</i>
                    Slaptažodis
                </label>
                <input type="password" id="password" name="password" placeholder="Įveskite slaptažodį" required>
            </div>
            
            <div class="form-actions">
                <button type="submit" class="btn btn-gradient btn-full">
                    Prisijungti
                </button>
            </div>
            
            <div class="auth-links">
                <p>Dar neturite paskyros? 
                    <a href="/register" class="link">Registruokis</a>
                </p>
            </div>
        </form>
    </div>
<%- include('partials/footer') %>